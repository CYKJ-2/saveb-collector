# 自动采集积压修复（2026-09-14）

## 原因和修复行为

原 refresh 在近期日期之后追加全部 Pending / Reversed / Failed / Expired 单号及历史滚动日。三千多个分片需要数小时；调度器等待上一轮 refresh 完成，跨午夜仍无法提交新一天任务。原 refresh 的 params 未填写实际 start/end，管理页因此显示空日期范围。

新版默认 refresh 只采今天、昨天，按北京时间固定日期，先采今天；该任务成功即可更新首页成功时间。自动间隔仍取 `collector.schedules.interval_minutes`，默认 30 分钟，Beat 每 60 秒检查到期时间。跨午夜不复用昨天的任务。

历史单号与滚动日另建 mode=history、params.task_kind=open_recheck 的任务；同账户、相同预览/发布模式已有活跃复查任务时复用，避免每半小时新增数千分片。尚未采过的新 Pending 单号在下一轮复查规划时加入。历史复查可能运行数小时；在每个分片开始前让位给近期任务。账户锁仍保证同一时刻只有一个来源抓取/提交流程，正在执行的历史分片不会被强制打断。

`pending` 的三月、四月等历史范围是独立 Pending 发现窗口，属于防漏机制，并不表示当天任务的日期。Admin 列表区分“字段刷新”“历史 Pending 发现”“历史订单复查”。历史复查不是单一日期范围，具体单号和滚动日见详情。

## 服务器升级

先将本地 **saveb-collector** 的本次代码提交并 push，再在服务器执行以下命令。沿用服务器 git pull 后构建的方式，不需要 GHCR。保留 `.env`、已有数据库及附件。

```bash
cd /home/admin_chen/www/saveb-collector &&
git pull --ff-only origin main &&
export RELEASE_IMAGE="saveb-collector:server-$(git rev-parse HEAD)" &&
docker build \
  --build-arg PYTHON_IMAGE=public.ecr.aws/docker/library/python:3.12-slim-bookworm \
  -t "$RELEASE_IMAGE" . &&
docker compose -f docker-compose.server.yml stop beat worker history maintenance logistics api &&
docker compose -f docker-compose.server.yml --profile tools run \
  --rm --no-deps --pull never migrate &&
docker compose -f docker-compose.server.yml up \
  -d --no-build --pull never --wait api worker history maintenance logistics beat &&
curl -f http://127.0.0.1:18085/ready
```

`migrations/006_separate_realtime.sql` 仅把**尚未结束且混入历史单号/滚动日的旧 refresh** 改为历史任务。任务 ID、操作者、成功分片、归档和业务订单不变；剩余分片继续执行，并将受影响账户下次调度时间设为现在。重复迁移不会重复推进计划。须在旧 worker/Beat 停止后迁移；新版健康检查要求 schema version 6。

存在上述旧积压任务时，启动后约一分钟内应出现当前日期的自动任务；没有旧积压任务则继续按已有下次调度时间执行。例如 9 月 14 日应显示 `2026-09-13 ~ 2026-09-14`、共 2 片。来源响应时间决定完成时间；若需要立即补今天，也可点击首页“采集当天数据”。新服务健康只证明依赖就绪，最终还需核对任务成功及订单管理中的数据。

前端文字更新：将本地 **saveb-admin** 两个前端文件提交并 push，然后在服务器执行：

```bash
cd /home/admin_chen/www/saveb-admin &&
git pull --ff-only origin main &&
export RELEASE_IMAGE="saveb-admin:server-$(git rev-parse HEAD)" &&
docker build \
  --build-arg NODE_IMAGE=public.ecr.aws/docker/library/node:22-bookworm-slim \
  --build-arg NGINX_IMAGE=public.ecr.aws/docker/library/nginx:stable-alpine \
  -t "$RELEASE_IMAGE" . &&
docker compose up -d --no-build --pull never --wait
```

saveb-api 无需为此次修复重建。旧 ERP 服务和数据库无需停止或重新导入；不执行 down -v、队列清空、业务表清空或强制覆盖本地配置。

## 验证范围

回归覆盖 3286 条历史订单积压时近期任务仍为 2 片、下个自动周期继续创建、跨午夜创建当前日期任务、旧任务重复迁移不丢分片、历史 Pending 后续变 Completed 仍写入创建日期且保留人工修改。集成测试仅对独立 `collector_test` 数据库运行，抓取使用模拟响应。
