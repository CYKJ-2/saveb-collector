# 旧脚本迁移核对清单

> 物流实施更新（2026-09-08）：采购入库物流的 AfterShip／快递 100 主动查询已迁入 Collector，并接入 Admin 采购工作台。旧出库包裹和 webhook 不包含在本次范围；详见 [物流采集说明](物流采集说明.md)。

> 后续实施更新（2026-09-08）：本清单原始盘点之后，已补上历史 Pending 专用扫描、采集对账和备份恢复验证。实现、调用方式与运行限制见 [采集巡检与备份说明](采集巡检与备份说明.md)。下文“缺口/未迁移”保留为实施前盘点记录，这三项应以新增说明为准。

检查日期：2026-09-08。范围：本地 saveb-deploy、saveb-erp、saveb-sales-data，并对照 saveb-collector 及相关 saveb-api 接口。只做静态检查，没有运行旧采集、导入、改价、物流或备份脚本，也没有改动业务数据。

本清单中的“存在”表示本地源码实现，不证明旧线上现在仍启用；outputs 下的发布副本、备份文件、编译产物与 node_modules 不重复计算。脚本目录之外的 server.js 内置定时任务也纳入检查。

## 一、结论与优先考虑项

订单主采集链路已迁移，但不能把整个旧系统都视为已迁入 Collector。最值得决定的项目是：

| 优先级 | 功能 | 当前缺口 | 建议归属 |
| --- | --- | --- | --- |
| 高 | 历史 Pending 发现与分批复查 | 有历史回查和已知订单复查，但没有旧版 Pending 专用周区间扫描/覆盖证明；已知订单逐笔复查会使单轮时间较长 | Collector，改造为独立任务和预算，不直接照搬旧缓存 |
| 高 | 来源 → 订单 → 统计的对账报告 | 有分页/事务校验和测试，但没有等价的运行期多维对账清单 | Collector 做采集对账，API 做业务汇总对账 |
| 高 | 数据库、归档与附件备份/恢复验证 | Collector 没有配套的计划备份及恢复演练脚本 | API/统一运维，备份整个共享库，不能只备份 Collector |
| 按业务需要 | 物流自动查询 | AfterShip、快递 100 查询及回调没有迁入当前采集链路 | Collector 独立物流任务 + API 物流事件与回调 |
| 按业务需要 | PayPal Excel 批量导入 | 没有余额、审核数、提现表的同等批量解析/匹配流程 | API 财务导入；大型任务可由独立 worker 处理 |
| 按规则维护方式决定 | Excel 站点/员工规则同步 | 已支持规则读取，但不再自动扫描旧桌面 Excel | API 规则导入和版本管理，Collector 读取已确认版本 |
| 可选 | 原始订单/商品及多工作表 Excel 导出 | 有业务 CSV 和兼容 JSON 导出，但不等于旧版完整明细/多工作表报表 | API 报表导出；原始证据导出需单独权限 |
| 可选 | 归档重算正式发布 | 只有预览；不能从旧归档直接发布重算结果 | Collector，需要明确可更新字段及人工调整保护 |
| 较低 | 独立外部巡检 | 已有首页状态和指标，缺少旧 PowerShell 的登录/页面/API 联合巡检循环 | 独立运维脚本 |
| 独立项目 | 商品相册采集、AI 定价、批量改价/下架 | 未迁移，且不是订单采集能力 | 商品管理工具或独立 Collector source，不与收单任务混用 |

## 二、已迁移或已有替代，不必再搬一次

| 旧能力及证据 | 当前对应 | 判断 |
| --- | --- | --- |
| [saveb_sales_refresh_once.sh](../saveb-deploy/scripts/saveb_sales_refresh_once.sh)、[dh_order_dashboard_fetch.cjs](../saveb-deploy/scripts/dh_order_dashboard_fetch.cjs) | app/collectors/dh_order.py、app/services/collection.py | DH-Order 分页采集、响应校验、原始归档和业务更新已迁移 |
| server.js 的 scheduledRefresh、queueRefreshData | app/queue/celery_app.py、app/queue/tasks.py、collector.schedules | 定时、手动、互斥、任务持久化已替代；间隔可以在管理页设置 |
| [backfill_dh_order_dashboard_history.py](../saveb-deploy/scripts/backfill_dh_order_dashboard_history.py)、run_backfill_all_history.ps1、run_backfill_2026_weekly.ps1 | scripts/backfill_history.py、范围更新/仅补缺页面 | 按日期补采及失败续跑已迁移；旧周批次包装不必原样迁入 |
| Chrome Cookie 抽取与 shell 续期 helper | app/collectors/session.py | 已用账号密码、本地验证码识别、Redis 会话共享替代，不需要旧浏览器配置路径 |
| [fetch_dashboard_exchange_rates.py](../saveb-deploy/scripts/fetch_dashboard_exchange_rates.py) | app/collectors/exchange_rates.py | Frankfurter 汇率采集已迁移，写 collector/API 汇率表 |
| dh_order_dashboard_fetch.cjs 的站点归类、员工分摊、商品图片计数 | app/domain/orders.py、app/collectors/product_images.py | 主能力已迁移；商品页面抓取现在要求 productImageHosts 白名单，不能假设所有旧站点已启用 |
| [rebuild_chart_from_server.cjs](../saveb-deploy/scripts/rebuild_chart_from_server.cjs)、aggregate_dh_order_dashboard_history.py | app/persistence/projections.py + API 首页统计服务 | 采集时更新受影响日期；不再执行旧 server.js 重建文件 |
| refresh_status.json、首页 refreshText | API CollectorService + Admin CollectorStatus.vue | 状态、成功完成时间、配置间隔、任务进度和调度心跳已迁移 |
| latest/history 文件输出 | scripts/export_legacy.py | 有可选版本化 JSON 导出，但仅导出兼容快照，不是旧目录完整镜像或完整原始归档备份 |

## 三、部分迁移的具体差异

### 1. 历史 Pending 发现、覆盖进度与任务预算

旧 [dh_order_dashboard_fetch.cjs](../saveb-deploy/scripts/dh_order_dashboard_fetch.cjs) 的 fetchPendingCoverage：

- 来源接口用 paymentStatus=Pending，按 7 天区间扫描历史。
- 默认每轮一个区间；区间最多 10 页，总分页和执行时间有预算。
- pending_coverage_cache.json 保存已扫描区间、下一起点、完整性状态，并校验区间连续。
- PENDING_COVERAGE_BACKFILL_ONLY 支持只补这一类覆盖缓存。

当前 [app/services/jobs.py](app/services/jobs.py) 的 refresh：最近 3 天、数据库中已知的 pending/reversed/failed/expired 订单逐笔复查、一个历史日游标。尚未发现的历史订单主要依靠滚动回查或人工范围更新，没有 Pending 专用扫描与独立覆盖进度。

这不代表 Collector 完全不会发现历史 Pending，而是发现路径、覆盖表达和工作量不同。旧缓存也不能简单当作持续新鲜的数据：旧区间扫描完成后主要向新日期延伸，不能据此证明以前缓存的订单状态仍准确。

建议迁移其“分区发现、分页预算、覆盖证明”的思想，同时把历史发现与已知订单复查拆出实时任务；批量规模、频率和优先级独立配置。当前轮次长的问题不适合靠重复加入所有历史扫描解决。

### 2. Excel 站点/员工规则输入

旧 [export_dh_order_site_rules.py](../saveb-deploy/scripts/export_dh_order_site_rules.py) 从桌面指定命名 Excel 的主站、网红、线下工作表读取域名和员工代码，叠加自定义映射、influencer_domain_map.json 后写 site_rules.json；旧 shell 会在抓取前执行它。

当前 [app/services/context.py](app/services/context.py) 从 API system_state 中的 legacy_dashboard_context、确认有效的 influencer_domains、Collector 汇率及可选配置文件加载规则。它没有继续监视旧 Excel。

如果业务仍通过 Excel 修改站点/员工名单，需要新增“上传 Excel → 校验差异 → 确认规则版本”的 API 功能。若已统一在新系统维护规则，则无需保留旧文件扫描方式。尤其不能假设所有员工代码都会自动从用户表变成采集规则。

### 3. 归档修复与正式重算

旧 [repair_offline_staff_allocations.cjs](../saveb-deploy/scripts/repair_offline_staff_allocations.cjs) 重新解析人员分配并写回 history/latest；[reclassify_dh_order_history_offline_sites.py](../saveb-deploy/scripts/reclassify_dh_order_history_offline_sites.py) 对历史归类进行改写。

新系统会在重新采集时规范化并更新受影响日期；归档 reprocess 只预览、不发布。仍缺少“无需访问来源，直接用归档修正规则后发布”的流程。

这是可选的维护能力，不是日常采集必需。若要迁移，应保留来源版本、受影响订单清单、人工调整保护与发布审计，不能把旧脚本直接改 JSON 的方式照搬到业务库。

## 四、真正未迁移的其他任务

### 4. 物流采集：AfterShip、快递 100

证据：[saveb-deploy/server.js](../saveb-deploy/server.js) 的 refreshShipmentTracking、refreshKuaidi100ShipmentTracking、startLogisticsMonitor、scheduleKuaidi100TrackingRefresh，以及 /api/logistics/* 接口。

旧代码具有国际物流定时批量查询、快递 100 自动/手动查询、状态缓存和 AfterShip webhook 处理；是否启用依赖当时配置，不能从源码认定线上已启用。

[saveb-erp/api/src-ts/routes/extensions.ts](../saveb-erp/api/src-ts/routes/extensions.ts) 明确将物流刷新及 webhook 返回 503/not_enabled；当前 API 有采购物流单号字段，但本次未找到等价外部自动查询实现，Collector 也没有物流 source。

如果采购/仓库需要自动得知运输和签收进展，这项有实际业务价值。查询可用独立队列；webhook、物流事件与采购状态映射由 API 负责，不应与收单登录共享 Cookie 或队列预算。

### 5. PayPal 批量数据导入

旧脚本：

- [import_paypal_current_balances.py](../saveb-deploy/scripts/import_paypal_current_balances.py)：Excel 当前余额解析及账户匹配。
- [import_paypal_review_counts.py](../saveb-deploy/scripts/import_paypal_review_counts.py)：审核数表导入。
- [import_paypal_total_withdrew.py](../saveb-deploy/scripts/import_paypal_total_withdrew.py)：累计提现表导入。
- [import_july_withdrawals.js](../saveb-deploy/scripts/import_july_withdrawals.js)：特定月份提现记录写入。

这些是人工文件导入，不是登录 PayPal 网站自动抓余额。旧脚本包括别名/相似度匹配和本地状态写入，不应直接用于当前库。

当前 API 有 PayPal 管理和导出，但本次未找到同等 Excel 批量导入流程。若需要，优先做 API 财务导入，匹配结果需可复核；特定月份脚本应泛化，不应长期保留硬编码月份。

### 6. 对账、异常审计

旧 [dual-run-compare.js](../saveb-erp/scripts/dual-run-compare.js) 对比旧新系统指定日期、范围、人员、PayPal、月统计等；[audit-migration-ambiguities.js](../saveb-erp/scripts/audit-migration-ambiguities.js) 检查外部订单号重复及采购关联歧义；[audit_payment_link_orders.cjs](../saveb-deploy/scripts/audit_payment_link_orders.cjs) 检查支付链接订单分类。

Collector 的分页计数、来源版本、事务和单元/集成测试只能覆盖采集过程的一部分，不等于这些业务口径的端到端对账。

建议增加只读对账报告：按日期/币种/状态比较归档规范化结果、来源记录、API 订单和统计，单独标明人工覆盖、软删除及预览造成的合理差异。不能将所有差异一键覆盖。

### 7. 备份与恢复演练

旧 [saveb-erp-backup.sh](../saveb-erp/ops/backup/saveb-erp-backup.sh) 导出 PostgreSQL、打包附件、保存元信息和校验和；[saveb-erp-restore-verify.sh](../saveb-erp/ops/backup/saveb-erp-restore-verify.sh) 在隔离环境恢复校验；[saveb-erp-backup.cron](../saveb-erp/ops/backup/saveb-erp-backup.cron) 有每日计划配置。

当前 Collector 没有同等自动备份和恢复验证脚本。它与 API 共用业务库，应统一备份 orders、业务表、collector 原始归档/任务和附件，不能把 export_legacy.py 当作全库备份。建议放统一运维层；本次未检查外部服务器是否另有备份系统。

### 8. 独立巡检循环

旧 [saveb_sales_health_check.ps1](../saveb-deploy/scripts/saveb_sales_health_check.ps1) 检查登录页、登录会话、采集状态、日期数据、PayPal API，并输出日志和告警文件；[saveb_sales_health_monitor_loop.ps1](../saveb-deploy/scripts/saveb_sales_health_monitor_loop.ps1) 循环执行。

Collector 有 /health、/ready、/metrics 和首页监控，API 有 check_collector.php，但没有整套等价外部登录巡检循环。可以适配为针对新 API 的运维巡检。旧“告警”主要是本地报告文件，不能说旧版已有短信或邮件推送；外部推送属于新增需求。

### 9. Excel、商品明细导出

旧 [export_dh_order_products.py](../saveb-deploy/scripts/export_dh_order_products.py) 输出原始 JSONL、订单 CSV、商品项 CSV 和 Excel；[export_dh_order_dashboard_excel.py](../saveb-deploy/scripts/export_dh_order_dashboard_excel.py) 生成分类、员工销售、币种、订单明细和付款状态多工作表报表。另有指定日期报表、图表和人员销售辅助脚本。

API 已有订单、SA 销售、采购、PayPal 等 CSV 导出，不应描述成新系统完全没有导出。缺口是旧版多工作表 Excel、独立商品行导出及原始证据导出。按实际报表需要决定，报表通常放 API，避免采集任务等待 Excel 生成。

### 10. 商品相册抓取、AI 定价与批量改价

代表脚本：fetch_album_all_titles.mjs、szwego_fetch_batch.mjs、process_zhishang_album.mjs、download_saveb_lv_products_with_images.py、price_*_ai.py、compare_david_*、build_lv_*、update_saveb_prices_by_product_id.py、update_saveb_*_prices_and_disable.py 等。

这组功能包含相册分页抓取、商品图下载、材质/款型分析、定价表和图片矩阵、旧商城后台改价/禁用商品。本次未发现 Collector 有对应功能。

这些不是收单采集遗漏，应先明确是否仍使用旧商品后台和定价流程，再另立商品数据源或商品管理工具。部分旧代码含固定本机路径、浏览器依赖和嵌入式凭据，不宜原样迁移；本报告不收录凭据。

## 五、不建议放进 Collector 的旧 ERP 工具

- [migrate-json-to-pg.js](../saveb-erp/scripts/migrate-json-to-pg.js)：导入订单、Invoice、操作日志、PayPal、网红、汇率、采购和兼容快照，并进行迁移对账。属于一次性整库迁移，不应每次采集运行。
- [backfill-canonical-orders-from-legacy.js](../saveb-erp/scripts/backfill-canonical-orders-from-legacy.js)：旧快照向规范订单补齐，不是从来源重新采集。
- import-users.js、provision-sa-client.js：账户与权限初始化，属于 API 管理。
- [worker/src-ts/index.ts](../saveb-erp/worker/src-ts/index.ts)、processor.ts、ocrClient.ts：主要为 Invoice OCR 队列，非收单队列。当前 API 已有 Invoice OCR 接口及外部引擎/本地回退；旧 BullMQ 队列、熔断等不等于已原样迁移，但应在 Invoice 子系统单独评估。
- generate-manifest.js、generate-sbom.js、build-release-package.py、回滚/发布验证与 Hostinger 部署脚本：发布工程和旧平台运维，不应混入采集业务。

## 六、saveb-sales-data 的实际性质

本地该目录是数据目录，本次未发现可执行采集脚本：

| 文件 | 数据性质 | 与 Collector 的关系 |
| --- | --- | --- |
| completed_orders_*.json / .csv | 旧订单数据 | 如需补迁旧存量，用一次性导入及对账，非日常文件中转 |
| influencer_domain_map.json | 网红域名映射 | 对应新系统已确认的网红域名/规则上下文，需核对内容而非复制文件 |
| invoice_orders.json、invoice_operation_logs.json | Invoice 和操作记录 | 属于 API Invoice 模块 |
| paypal_shared_state.json | PayPal 人工维护状态 | 属于财务模块，不应被 DH-Order 采集覆盖 |
| procurement_orders.json、procurement_workbench_audit.jsonl | 采购与审计 | 属于 API 采购模块 |
| latest_good.json、manifest.json 与备份副本 | 旧快照/清单/备份 | 文件本身不是待迁移脚本；不能据存在推断当前运行链路 |

本次没有逐笔比较上述存量文件与当前数据库，因此不宣称这些文件中的全部记录已经迁完。若用户要确认存量完整性，需要另做只读数据对账。

## 七、建议决定顺序

先决定历史 Pending 的发现/复查策略和只读对账；同时建立统一备份恢复验证。物流和 PayPal 导入按采购/财务实际使用情况决定。规则 Excel 输入、Excel 导出与归档正式重算按使用习惯选配。商品定价、旧浏览器续期、Hostinger 打包及整库导入不直接合并到日常收单任务。

本次只生成此清单，未实施任何迁移。
