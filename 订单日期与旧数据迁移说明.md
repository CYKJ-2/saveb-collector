# 订单日期与旧服务器数据迁移

2026-09-08 起采用 `source-create-time-v2`。

## 自动回查范围的来源

当前 Collector 的 `JobRequest.lookback_days=2`，自动 `refresh` 每轮规划当天和前一天两个日期，CLI 默认也为 2 天。此前为 3 天，现按数据量缩短窗口；已创建任务保留原分片，新建默认任务使用两天窗口。旧 `saveb_sales_refresh_once.sh` 向抓取脚本传入当天；旧抓取脚本查询时前后各扩一天，再按目标日期筛选，另外扫描 Pending。这不等于当前明确重采两个日期的逻辑。

## 字段与日期口径

| 数据库字段 | 来源 / 用途 |
| --- | --- |
| `orders.order_time` | 使用 DH-Order `createTime`，供订单日期筛选及现有按订单日期统计；付款或更新时间变化不会使订单移日 |
| `orders.source_created_at` | 来源创建时间，单独保存权威 `createTime`；与人工调整过的业务日期可以不同 |
| `orders.payment_time` | `paymentTime` / `payTime` / `paidTime`，没有则为空；后续响应缺省不会清空已保存值 |
| `orders.completed_time` | `completeTime` / `completedTime`，没有则为空 |
| `orders.source_updated_at` | 来源 `updateTime`，用于记录来源更新时间；与应用自身 `updated_at` 不同 |
| `orders.legacy_accounting_time` | 修正前的旧业务日期，保留供核对；不是新的收入统计日期 |

例如：9 月 1 日下单，9 月 3 日付款，订单一直归在 9 月 1 日；变为 Completed 后，现有按订单日期的销售统计计入 9 月 1 日，而不是移动到付款日。付款时间独立保留，可用于后续付款日维度报表。Invoice 自己的日期口径不变。

原始响应仍保存在 `collector.chunks.raw` 和 `collector.source_orders.raw`。人工调整字段及人工软删除保护继续保留；已能通过 Collector 投影基线识别的人工日期调整也保留，不被自动日期修正覆盖。

升级时，未完成的旧采集任务在执行下一个分片前更新日期策略及上下文版本，保留原先冻结的分类规则和汇率，避免旧任务继续写回最大更新时间。已归档的历史分片不改写。

## 升级顺序

1. 先执行 API 新增可空字段迁移：

```powershell
docker exec saveb-api-app php artisan migrate --path=database/migrations/2026_09_08_120000_separate_order_source_times.php --force
```

2. 在 Collector 项目目录构建和迁移：

```powershell
docker compose build api
docker compose run --rm migrate
```

Collector `004_order_dates.sql` 新建 `collector.order_date_repairs`，版本升至 4；若缺少 API 日期字段，明确拒绝迁移并提示先执行 API 迁移。

3. 预览已有数据修正：

```powershell
docker compose run --rm api python scripts/repair_order_dates.py
```

4. 完成备份后应用修正，并启动新版服务：

```powershell
docker compose stop worker history maintenance beat
docker compose run --rm api python scripts/repair_order_dates.py --apply
docker compose up -d --no-build api worker history maintenance beat
```

修正默认只预览；`--apply` 才写入。每批 100 条事务提交，使用与采集相同的账户锁和日期锁；失败后可重新运行，已修正数据不会反复移日。应在低峰运行，批次之间整个历史范围尚处于逐步修正状态。

## 从旧服务器导入数据

**旧数据不需要有新增的五个字段。** 它们全部 nullable，无非空默认要求；原来的 `order_time`、`raw`、订单号及身份字段仍可正常导入。

支持保留字段名的 SQL/COPY 导入，例如 PostgreSQL `pg_dump --column-inserts` 或带列清单的 COPY。不要靠两边表的列位置做映射。整库 schema 恢复后也要再运行上述 API 与 Collector 迁移。

导入完成后执行 `repair_order_dates.py` 预览，再加 `--apply` 完成日期归一化：

- 优先使用该账户 `collector.source_orders.raw.order.createTime`。
- 其次使用业务订单 `raw.originalCreateTime` 或 `raw.order.createTime`。
- 只有旧 `order_time` 或旧展示字段 `raw.createTime` 时，无法证明它是真实创建时间：保留旧日期，计入 `missing_creation_evidence`。后续真实采集提供权威 `createTime` 后可补齐。
- 非法时间计入 `invalid_time`，不使其他合法订单导入失败；修正脚本跳过该条。
- 人工软删除跳过；发现人工日期调整时保留；发生并发 API 修改时跳过该条，计入 `concurrent_skipped`，稍后重跑核对。
- `classification=invoice` 的旧镜像订单单独跳过，计入 `invoice_skipped`；收单统计重建也排除它们，保留独立 Invoice 统计。
- 修正只更新日期字段和相关 raw 展示日期，不改金额、客服、客户信息等人工维护内容；同时保存修正前日期、更新投影基线并重建受影响日期统计。
- 无法从 SQL 覆盖导入恢复已被覆盖的本地人工调整：需先确定导入方案。仓库旧 `copy-orders-data.ps1` 会清空目标 orders 表，它是替换式迁移工具，不是保留本地修改的增量导入工具。

## 核对

```sql
SELECT count(*) AS repaired_rows FROM collector.order_date_repairs;
SELECT order_id,before_value,after_value,created_at
FROM collector.order_date_repairs ORDER BY id DESC LIMIT 20;
```

```powershell
docker compose exec api python scripts/reconcile.py --start 2021-08-10 --end 2026-09-08
```

日期修正不能创造旧系统未采到的订单。导入后仍有历史缺单时，需要“范围更新”重新获取全状态订单，不能只靠“仅补缺”或归档日期修正。

## 本次本地执行结果

修正前已完成全库及 638 个附件的真实恢复验证。扫描 10,809 条订单，补齐 10,779 条收单订单日期信息，其中 8,032 条业务日期恢复为来源创建日期，30 条 Invoice 镜像跳过；没有非法时间或缺少创建证据的收单订单。修正日志写入 `collector.order_date_repairs`，受影响的统计与首页快照已重建。

修正后再次预览为 0 条待改；2021-08-10 至 2026-09-08 的全范围对账通过，分类统计和首页快照汇总差异均为 0。来源归档关联检查覆盖本地已有的 552 条来源记录，不表示所有旧导入订单均已重新从上游采集。
