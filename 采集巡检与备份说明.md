# 历史 Pending、采集对账与备份恢复验证

后续日期升级使用 schema version 4；API 字段迁移和旧数据导入兼容步骤见 [订单日期与旧数据迁移说明](订单日期与旧数据迁移说明.md)。下文 version 3 是这三项功能初次安装的迁移版本。

实现日期：2026-09-08。适用于直接写入 saveb-api 同库的 Collector。原有首页当天采集、可配置刷新间隔、范围更新、人工调整保护均继续使用。

## 1. 升级与调度入口

在 saveb-collector 目录运行：

```powershell
docker compose build api
docker compose run --rm migrate
docker compose up -d --no-build api worker history maintenance beat
```

迁移 `migrations/003_operations.sql` 新建 `collector.pending_state`、`collector.pending_coverage`、`collector.reconciliation_reports`，记录 schema version 3。必须先迁移再启动新版服务，`/ready` 会验证版本。

| 工作 | 定时任务写在哪里 | 默认时间 | 执行者 |
| --- | --- | --- | --- |
| 原有字段刷新 | `app/queue/celery_app.py` → `collector.schedule` → `collector.schedules` | Beat 每 60 秒检查；实际间隔由管理页配置 | collect / history 队列 |
| 历史 Pending 发现 | 同文件 → `collector.pending_schedule` → `app/services/pending.py` | 每 60 秒检查，默认每 30 分钟提交一个历史区间 | maintenance 提交，history 执行 |
| 分片对账 | `app/services/collection.py` → `app/services/reconciliation.py` | 非预览、正式发布的分片提交时 | 对应采集 worker |
| 全范围对账 | `app/queue/celery_app.py` → `collector.reconcile` | 每天北京时间 03:10，从 `SAVEB_HISTORY_START` 到当天 | maintenance |
| 全库备份并恢复验证 | `scripts/install_backup_task.ps1` → `scripts/backup_daily.ps1` | 安装后每天本机时间 02:15 | Windows 任务计划程序 |

只运行一个 Beat。Pending 的 30 分钟与首页“自动采集间隔”是两个独立配置，管理页当前只修改原有字段刷新间隔。

## 2. 历史 Pending 专用发现

### 行为与进度

向 DH-Order `/order/list2` 提交 `paymentStatus=Pending`，按历史日期区间分页查找此前本地未知的 Pending 订单。请求保留旧协议日期两侧 padding；返回订单可能超出名义区间，仍使用既有规范化和订单号合并逻辑。

默认从 `SAVEB_HISTORY_START` 开始，每轮 7 天，每个分片最多 10 页（每页 100 条），HTTP 采集阶段上限 240 秒。超过分页或时间预算时自动将多日区间对半拆分；如果单日仍超预算，则按现有重试规则失败，不跳过。可以调高 `SAVEB_PENDING_MAX_PAGES` / `SAVEB_PENDING_TIMEOUT_SECONDS` 或检查来源结果，再续跑。即使来源意外返回其他状态，Pending 任务也只选择标准化后仍为 pending 的订单发布。

原有当天/刷新任务优先执行。正在运行的 Pending 任务不会被自动重复堆积。只有整个自动区间成功后才推进游标；失败、取消、预览及人工范围扫描都不推进自动游标。扫描到当天后从历史起点开始下一轮。首次完整扫描不是一次完成，较长历史范围需要多轮。

Pending 扫描只发现当前仍 Pending 的订单；已知订单变为 Completed 等状态，继续由原有未完成订单复查处理。

### 配置

在 `.env` 中设置，修改后重新创建运行容器：

```dotenv
SAVEB_PENDING_ENABLED=true
SAVEB_PENDING_INTERVAL_MINUTES=30
SAVEB_PENDING_WINDOW_DAYS=7
SAVEB_PENDING_MAX_PAGES=10
SAVEB_PENDING_TIMEOUT_SECONDS=240
```

### 手动扫描与恢复

```powershell
docker compose exec api python scripts/discover_pending.py --start 2026-08-01 --end 2026-08-31 --wait
docker compose exec api python scripts/discover_pending.py --start 2026-08-01 --end 2026-08-31 --dry-run --wait
docker compose exec api python scripts/discover_pending.py --resume JOB_ID --wait
```

无 `--wait` 只表示提交成功；等待超时不取消后台任务。现有 Collector 任务 API 也接受 `mode=pending` 及起止日期，仍需服务令牌、操作人和幂等键。Admin 未新增 Pending 专用按钮。

### 写入表

- 任务、分片原始响应及标准化结果：`collector.jobs`、`collector.chunks`、`collector.outbox`。
- 来源与变更：`collector.source_orders`、`collector.changes`。
- 自动游标、下次执行时间、循环数：`collector.pending_state`。
- 成功扫描区间及抓取数量：`collector.pending_coverage`。分片失败不写成功覆盖。
- 正式业务发布：`public.orders`、`public.daily_stats`、`public.legacy_dashboard_days`、相关 `public.system_state`。

**不会写 `collector.coverage`。** Pending-only 扫描不能证明某天所有订单采集完成，不能使“仅补未成功采集的日期”错误跳过该日期。人工调整及软删除保护复用原有合并逻辑。

```sql
SELECT * FROM collector.pending_state;
SELECT account,start_day,end_day,fetched,completed_at
FROM collector.pending_coverage ORDER BY completed_at DESC LIMIT 20;
SELECT id,status,error,created_at,updated_at
FROM collector.jobs WHERE mode='pending' ORDER BY created_at DESC LIMIT 20;
```

## 3. 采集对账

### 检查内容

1. 已发布的来源订单是否缺少业务订单；当前分片已选择的订单是否缺少来源归档。
2. 按北京时间业务日和分类，将有效 Completed 订单数量、商品数量、美元金额与 `daily_stats` 的分类总计比较。员工分摊行不会再加一次，invoice 独立统计行不参与。
3. 将有效 Completed 订单汇总与 `legacy_dashboard_days.payload.totals` 比较。
4. 人工调整和软删除作为信息计数，不因与来源不同就判为采集错误。统计仍以人工调整后的有效业务订单为准。

报告保存在 `collector.reconciliation_reports`。不自动修复、不改订单或统计，不调用收单系统；它验证本地归档到业务和汇总的链路，不能证明收单系统未返回的订单也已全部采到。当前不逐字段核对所有商品明细、员工分摊或首页所有数组。

### 对账与采集成功的关系

对账差异标记为 `warning`，订单采集本身成功仍为 `succeeded`，不会再次把“没有新订单”显示为采集失败。分片 counts 中含 `reconciliation_id` 与 `reconciliation_status`。对账数据库执行错误会触发分片事务回滚和重试，保证发布与报告一致。

预览或 shadow 模式不进行正式业务分片对账；shadow 模式也不执行每日正式业务对账。

```powershell
docker compose exec api python scripts/reconcile.py --start 2026-09-01 --end 2026-09-07
# 可选保存 JSON 文件（以下路径在容器内）
docker compose exec api python scripts/reconcile.py --start 2026-09-01 --end 2026-09-07 --output /tmp/reconciliation.json
docker compose cp api:/tmp/reconciliation.json ./reconciliation.json
```

退出码：0 为通过，1 为发现差异。每次手动执行也写报告表；差异明细最多展示各 100 项，总差异数完整保存。历史人工更改或旧系统统计不一致也可能产生 warning，应先查看日期和差异类型，不要直接批量覆盖订单。

```sql
SELECT id,kind,job_id,start_day,end_day,status,created_at
FROM collector.reconciliation_reports ORDER BY id DESC LIMIT 30;
SELECT report FROM collector.reconciliation_reports WHERE id = 123;
```

## 4. 全库与附件备份、恢复验证

### 备份范围与产物

`scripts/backup_restore.py` 读取现有 `.env` 数据库配置。使用 PostgreSQL 导出的同一只读快照完成全库 `pg_dump` 与逐表计数，包含 public 和 collector 等所有应用 schema；不是只备份采集表。

同时备份 API 的附件存储目录。本地 Docker 默认附件在 `saveb-api-app:/data/attachments` 的命名卷中，不能只备份代码目录。

每次生成独立目录：

```text
backups/<UTC时间-随机后缀>/
  database.dump
  attachments.tar.gz
  manifest.json          # 同快照逐表行数、附件逐文件哈希、归档 SHA-256
  verification.json      # 真实恢复通过后才生成
backups/latest.json      # 最近一次完整验证成功
backups/last-attempt.json # 定时包装脚本的最近尝试结果
backups/backup.log       # 定时包装脚本日志
```

`backups/` 已加入忽略配置。当前不自动删除历史备份；需根据磁盘容量安排保留策略和异机复制。附件是文件系统快照，与数据库无法跨系统事务同步；尽量在低峰备份，并避免同时删除/替换附件。原生目录模式拒绝符号链接，归档读取也拒绝非普通文件和路径穿越。

### 本地 Docker 手动执行

宿主机需要 Python 环境已安装本项目依赖、Docker CLI 可用，数据库账号有备份读取及创建/删除临时库权限。这里使用数据库容器已有 PostgreSQL 16 工具，不依赖 Windows 安装 pg_dump。

```powershell
.venv/Scripts/python.exe scripts/backup_restore.py backup --pg-container saveb-api-postgres --attachments-container saveb-api-app --attachments /data/attachments --output backups
```

备份完成后自动创建随机 `collector_restore_verify_<UUID>` 数据库，执行真实恢复，比较全部应用表清单与行数，读取附件归档核对逐文件哈希，最后删除临时库。恢复发生在与来源相同的数据库服务器，需有足够临时磁盘空间。

**不提供覆盖现有业务库的恢复参数。** `verify` 只能创建内部生成的临时库；即使恢复出错，也会尝试删除该临时库。若容器/进程被强制终止，需由运维检查遗留的该前缀临时库，不能按模糊名称删除业务库。

重新验证已有备份：

```powershell
.venv/Scripts/python.exe scripts/backup_restore.py verify --pg-container saveb-api-postgres --backup-dir backups/实际备份目录
```

任意哈希、恢复命令、表清单或行数验证失败都会非零退出，不更新成功标记。数据库连接密码通过环境变量传给 pg 工具，不写入命令行、清单或日志。pg 工具详细 stderr 不直接公开，避免输出客户数据或连接信息。

### 安装 Windows 每日任务

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/install_backup_task.ps1
Get-ScheduledTask -TaskName Saveb-Collector-Backup-Verify
Get-ScheduledTaskInfo -TaskName Saveb-Collector-Backup-Verify
# 手动执行与定时任务相同的包装脚本
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backup_daily.ps1
```

默认每日本机时间 02:15，可通过 `-At 03:30` 修改，使用固定任务名更新，不重复创建。任务隐藏运行，禁止同任务重叠，并设置错过后补跑。使用当前登录用户的 Docker Desktop，因此用户需要登录且 Docker 正在运行；本机休眠/关机时不能持续备份。

### Linux / 原生 PostgreSQL

省略 `--pg-container`，使用 PATH 中与服务端相同或兼容更新版本的 `pg_dump`、`pg_restore`、`psql`；`--attachments` 指向真实本地目录。由系统 cron/systemd 调用，例如：

```sh
cd /opt/saveb-collector
.venv/bin/python scripts/backup_restore.py backup --attachments /data/attachments --output /var/backups/saveb
```

程序不会自动在 Linux 安装 cron，需在部署时配置。完整灾难恢复还需另外保管应用配置和凭据；本脚本不把 `.env` 打进备份，也不备份 PostgreSQL 集群账号/角色。

## 5. 本次本地验证记录

- 2026-09-08：全量测试 64 项通过，1 项需要额外测试 Redis 的可选队列测试跳过；隔离数据库名称为 `collector_test`，不在业务库执行测试清空操作。
- 新增迁移后真实备份恢复验证：69 张应用表的清单和行数与导出快照一致，638 个附件内容哈希一致，临时数据库已清理。
- Windows `Saveb-Collector-Backup-Verify` 已安装，并实际执行成功，退出码为 0。
- 2026-09-07 至 2026-09-08 的手动对账通过：228 条来源记录，无业务订单缺失，无分类统计/快照汇总差异，识别 1 条人工调整。
- 自动 Pending 任务已由 Beat 提交。完整历史覆盖需后续多轮运行，不以任务提交成功宣称全历史已经采完。一次 45 秒的近期多页 HTTP 探测超时，不影响已保存数据；正式任务使用上述独立预算和续跑机制。
