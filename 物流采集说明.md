# 采购物流查询

采购页 `http://localhost:3000/workbench/procurement` 提供“刷新物流”和单条查询，展示查询进度、物流状态、最近轨迹、检查时间和失败原因。刷新按钮需要 `business.procurement.logistics` 权限，列表仍需要 `business.procurement.list`；普通角色需在系统管理中授权后重新登录。

## 调用和存储

旧 `saveb-source` 的 `#purchasing-dept` 批量刷新优先调用 AfterShip，未配置时使用快递 100。新实现沿用这个优先级：Admin → saveb-api 权限校验 → Collector 持久化任务 → 独立 `logistics` worker → 物流供应商 → saveb-api 对应数据库。

- API：`GET /api/workbench/procurement/logistics/status`、`POST /api/workbench/procurement/logistics/refresh`（以路由实际统一前缀为准）。Collector 内部接口为 `/api/logistics/status`、`/api/logistics/refresh`，仅服务令牌可访问。
- `collector.jobs/chunks/outbox`：任务、每条采购记录的查询分片、可靠投递；任务模式为 `logistics`，不计入首页 DH 订单采集结果。
- `collector.logistics_schedule`：每个来源账户的下一次物流调度时间。
- `public.procurement_tasks.raw`：`deliveryStatus`、`trackingProvider`、`trackingProviderId`、`trackingQueriedNumber`、`trackingCheckpoint`、`trackingUpdatedAt`、`trackingLastCheckedAt`、`trackingError`、`deliveredAt`。同时递增记录版本。
- `public.shipment_tracking_events`：关联采购任务的标准化物流事件；相同事件幂等去重。
- `business_operation_logs`：手动触发操作记录。

当前范围是采购任务中供应商寄往仓库的 `tracking_no`，不是旧系统所有出库包裹或 AfterShip webhook 的完整迁移。查询不会改变采购流程状态、仓库状态或人工备注。查询期间单号、承运商或电话被修改，会跳过旧查询结果，避免覆盖新单号。

## 私有配置

仅编辑 Collector 的 `.env`，不要把密钥放进 Admin 或提交到源码：

```dotenv
SAVEB_AFTERSHIP_API_KEY=
SAVEB_KUAIDI100_API_KEY=
SAVEB_LOGISTICS_ENABLED=true
SAVEB_LOGISTICS_INTERVAL_MINUTES=30
SAVEB_LOGISTICS_BATCH_SIZE=20
```

两种密钥配置其中一个即可；两个都有时优先 AfterShip。快递 100 对接旧系统使用的 `stdio/queryTrace` API Key，不是传统接口的 customer/sign 配置。未配置密钥时页面明确提示未配置，不会伪报查询成功。

默认每 30 分钟查询最多 20 条，优先选择最久未检查的采购记录；自动任务跳过已签收、过期和间隔未到的记录。手动刷新允许重新查询这些状态，单条按钮只查指定采购记录。自动间隔独立于 DH 订单采集间隔，由上述环境变量控制；Beat 每分钟检查到期时间，具体逻辑位于 `app/services/logistics.py::schedule`，注册在 `app/queue/celery_app.py`。

异常最多尝试 3 次，鉴权失败或额度耗尽直接失败。页面同时显示成功、失败、跳过数量；请求被接受不代表所有物流查询已完成。查询仅发送单号、识别所需承运商以及适用的电话信息。供应商错误已脱敏，不保存密钥或带密钥的请求 URL。

## 升级与运行

先在 saveb-api 执行权限迁移，再在 Collector 目录执行：

```powershell
docker exec saveb-api-app php artisan migrate --force
docker compose build --build-arg PIP_NO_INDEX=1 api
docker compose run --rm migrate
docker compose up -d --no-build api worker history maintenance logistics beat
```

离线构建参数适用于已准备 `.wheels` 的本地环境；首次部署按 README 准备依赖。配置密钥后再次执行 `docker compose up -d --no-build api worker history maintenance logistics beat`，让环境变量在容器中生效。保持一个 Beat，并确保 `logistics` 服务运行。

```powershell
# 手动提交一批或指定采购记录，返回任务编号
docker compose exec api python scripts/refresh_logistics.py
docker compose exec api python scripts/refresh_logistics.py --task-id 123
docker compose logs --tail 80 logistics
```

旧服务器优先检查 `/etc/saveb/saveb-runtime.conf`。可用以下命令只列出包含变量的文件路径，再通过服务器编辑器查看：

```bash
sudo grep -RIlE 'AFTERSHIP_API_KEY|KUAIDI100_API_KEY' /etc/saveb /etc/systemd/system /opt /var/www /www/wwwroot 2>/dev/null
```

旧变量没有 `SAVEB_` 前缀，复制到 Collector 时需要加上。API Key 与 `AFTERSHIP_WEBHOOK_SECRET` 不同，后者不能用于主动查询。

## 验证范围

自动测试使用隔离数据库和模拟供应商，覆盖权限、缺少配置、AfterShip 已注册单号查询、快递 100 响应校验、事件去重、保留人工字段、查询中改号保护和失败重试。真实物流联调需要有效供应商密钥和实际单号。
